
// 临时修复：在API文件中直接定义配置
const TEMP_DOCUMENT_CONFIG = {
  MAX_CHUNK_SIZE: 400,
  BATCH_SIZE: 2,
  CONCURRENT_CHUNKS: 2,
  MAX_RETRIES: 3,
  RETRY_DELAY: 1500,
  CHUNK_DELAY: 1000,
  BATCH_DELAY: 1500,
  REQUEST_TIMEOUT: 25000
};

// 替换 CONFIG 的使用
const CONFIG = TEMP_DOCUMENT_CONFIG;
